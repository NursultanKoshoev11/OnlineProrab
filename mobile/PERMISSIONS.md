# Mobile Permissions

Planned permissions:

- Camera: attach receipt and construction progress photos.
- Photos: select existing receipt, material, and document images.

Release notes:
- Explain every permission in the store listing.
- Do not request permissions before the user opens a feature that needs them.
- Keep uploaded files private by default.
